# RealWorld reference application

This directory is an independently runnable, conventional JVM Clojure application. It is intentionally implemented without Phoenix tooling or generated code.

The reference application will establish behavior through its CLI and black-box tests before a web adapter or regeneration machinery is introduced.
